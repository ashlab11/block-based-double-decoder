# block-based-double-decoder

Use `uv` for environment management.

Quick start:

```bash
bash scripts/1_setup.sh
bash scripts/2_data.sh
```

Pick a PyTorch wheel family with `TORCH_CUDA_EXTRA`, for example:

```bash
TORCH_CUDA_EXTRA=cu128 bash scripts/1_setup.sh
```
